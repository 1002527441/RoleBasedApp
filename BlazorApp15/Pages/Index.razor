@page "/"

<PageContainer Title="Samples Manager" IsShowSearch="true" SearchClick="OnSearch">
</PageContainer>

<Radzen.Blazor.RadzenButton ButtonType="Radzen.ButtonType.Button"  Text="Add">

</Radzen.Blazor.RadzenButton>

<p>
    Search Text: @SearchText
</p>

@code{

    private string SearchText { get; set; }

    private void OnSearch(string searchText)
    {
        SearchText = searchText;
        StateHasChanged();
    }
}